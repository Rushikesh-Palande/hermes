// app.d.ts — ambient types for SvelteKit's `App` namespace.
//
// Fill in request locals, page data shape, platform bindings, etc. as
// they are introduced. Keeping the interfaces empty here (rather than
// removing them) means SvelteKit's generated types compile without
// warnings.

declare global {
    namespace App {
        // interface Error {}
        // interface Locals {}
        // interface PageData {}
        // interface PageState {}
        // interface Platform {}
    }
}

export {};
